require 'spec_helper'

describe PayJunction do
  it 'has a version number' do
    expect(PayJunction::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
